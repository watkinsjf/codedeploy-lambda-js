
version: 0.2
phases:
  install:
    commands:
      - npm install time
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket jwatkins-codedeploy-test --output-template-file outputSamTemplate.yaml --debug
artifacts:
  type: zip
  files:
    - samTemplate.yaml
    - outputSamTemplate.yaml


